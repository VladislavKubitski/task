# Uncomment the next line to define a global platform for your project
# platform :ios, '11.2'

target 'itexusTestt' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for itexusTestt
  pod 'RxSwift', '~> 4.0'
  pod 'RxCocoa', '~> 4.0'
  pod 'RxDataSources', '~> 3.0'

  pod 'Action', '~> 3.4'
  pod 'NSObject+Rx', '~> 4.1'
  
  # Realm database
  pod 'RealmSwift', '~> 3.0'
  pod 'RxRealm', '~> 0.7'


  target 'itexusTesttTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'itexusTesttUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
